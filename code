n = int(input("Que numero fibronnaci deseja encontrar ? "))
last=1
ant=1


if (n==1) or (n==2):
    print("1")
else:
    count=3
    while count <= n:
        termo = last + ant
        ant = last
        last = termo
        count += 1
    print("obtivemos o resultado". format(termo))
